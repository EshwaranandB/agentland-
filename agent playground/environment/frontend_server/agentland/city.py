import json

from django.http import HttpResponse

from .projection import project_city_frames


def city_page(session):
    """Render a read-only inspector and playback over confirmed event frames."""
    data = json.dumps(project_city_frames(session)).replace("</", "<\\/")
    return HttpResponse("""<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AgentLand City</title><style>
:root{--ink:#17212b;--paper:#edf4e9;--land:#b7dfb0;--road:#f6e7c4;--blue:#64a7d9;--good:#4b9d71;--warn:#c94d4d;--gold:#e2ad48;--muted:#66737c}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font-family:ui-monospace,SFMono-Regular,Consolas,monospace}.hud{padding:14px 5vw;background:var(--ink);color:white;display:flex;gap:16px;align-items:center;flex-wrap:wrap}.brand{font-weight:800;font-size:21px}.mission{max-width:430px}.metrics{margin-left:auto;font-size:12px}.controls{padding:10px 5vw;background:#dce9d8;display:flex;gap:9px;align-items:center;flex-wrap:wrap}.controls button{font:inherit;padding:6px 10px;border:2px solid var(--ink);background:white;cursor:pointer}.controls output{min-width:105px}.controls input{accent-color:var(--blue);flex:1;min-width:160px}.layout{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:18px;padding:18px 5vw}.city{min-height:555px;position:relative;overflow:hidden;background:var(--land);border:4px solid var(--ink);box-shadow:8px 8px 0 #8ba88a}.road{position:absolute;background:var(--road);border:3px solid var(--ink)}.road.a{left:5%;right:5%;top:49%;height:66px}.road.b{top:8%;bottom:8%;left:49%;width:66px}.building{position:absolute;width:190px;min-height:116px;padding:12px;border:4px solid var(--ink);background:#fff8e7;box-shadow:6px 6px 0 rgba(23,33,43,.22);cursor:pointer;text-align:left}.building h2{font-size:15px;margin:0 0 10px}.building small{display:block;color:var(--muted)}.town_hall{top:7%;left:9%;background:#d9e8f5}.code_factory{top:7%;right:8%;background:#f0ddd2}.testing_facility{bottom:8%;left:9%;background:#dfeee5}.deployment_tower{bottom:8%;right:8%;background:#eee0f0}.building.active{outline:5px solid var(--blue)}.building.progress{outline:5px solid var(--gold)}.building.blocked,.building.failed{outline:5px solid var(--warn)}.building.passed,.building.complete{outline:5px solid var(--good)}.worker{position:absolute;padding:7px 9px;border:3px solid var(--ink);background:white;font:700 12px inherit;cursor:pointer}.worker:before{content:'● ';color:var(--blue)}.worker.blocked:before{color:var(--warn)}.worker[data-location=town_hall]{left:34%;top:30%}.worker[data-location=code_factory]{left:68%;top:30%}.worker[data-location=testing_facility]{left:34%;top:69%}.side{display:grid;gap:15px;align-content:start}.panel{padding:13px;background:white;border:3px solid var(--ink);box-shadow:5px 5px 0 #b6c5bd}.panel h2{font-size:15px;margin:0 0 10px}.panel dl{margin:0;font-size:12px}.panel dt{color:var(--muted);margin-top:8px}.panel dd{margin:2px 0;overflow-wrap:anywhere}.feed{padding:0;margin:0;list-style:none;max-height:220px;overflow:auto}.feed li{padding:7px 0;border-bottom:1px solid #d8ddd4;font-size:12px}.seq{color:var(--muted)}.worker-list{display:grid;gap:7px}.worker-list button{font:inherit;text-align:left;padding:7px;background:#f6f2e8;border:2px solid var(--ink);cursor:pointer}.worker-list button[aria-selected=true]{background:#d9e8f5}@media(max-width:760px){.layout{grid-template-columns:1fr}.city{min-height:640px}.building{width:150px}.hud{padding:14px}.metrics{margin-left:0}}
</style></head><body><header class="hud"><div class="brand">AGENTLAND</div><div class="mission" id="mission"></div><div class="metrics" id="metrics"></div></header><nav class="controls" aria-label="Event playback"><button type="button" id="previous">Previous</button><button type="button" id="play">Play</button><button type="button" id="next">Next</button><output id="position"></output><input id="timeline" type="range" min="1" value="1" aria-label="Event playback position"></nav><main class="layout"><section class="city" aria-label="Event-derived AgentLand city"><div class="road a"></div><div class="road b"></div><button class="building town_hall" id="town_hall"><h2>Town Hall</h2><small>Session authority</small></button><button class="building code_factory" id="code_factory"><h2>Code Factory</h2><small>Builder execution</small></button><button class="building testing_facility" id="testing_facility"><h2>Testing Facility</h2><small>Fixed verification</small></button><button class="building deployment_tower" id="deployment_tower"><h2>Deployment Tower</h2><small>Artifact evidence</small></button><div id="workers"></div></section><aside class="side"><section class="panel"><h2 id="inspection-title">Inspection</h2><dl id="inspection"></dl></section><section class="panel"><h2>Workers</h2><div class="worker-list" id="worker-list"></div></section><section class="panel"><h2>Activity feed</h2><ol class="feed" id="feed"></ol></section></aside></main><script id="agentland-frames" type="application/json">""" + data + """</script><script>
const frames=JSON.parse(document.getElementById('agentland-frames').textContent),slider=document.getElementById('timeline'),position=document.getElementById('position');let index=frames.length-1,timer=null,selection={kind:'worker',id:'builder'};slider.max=frames.length;const text=value=>value===null||value===undefined||value===''?'None':String(value);const label=value=>String(value).replace(/_/g,' ');const row=(key,value)=>{const dt=document.createElement('dt'),dd=document.createElement('dd');dt.textContent=key;if(value&&typeof value==='object'&&value.url){const link=document.createElement('a');link.href=value.url;link.textContent=value.label;dd.appendChild(link)}else dd.textContent=text(value);return [dt,dd]};function inspectWorker(frame,worker){const task=frame.tasks.find(task=>task.id===worker.task);const data=[['Role',worker.role],['Status',worker.status],['Location',label(worker.location)],['Current task',task?task.title:'None'],['Runner',frame.hud.runner_identity],['Public action summary',worker.summary||'None'],['Files changed',frame.evidence.files.length],['Tool exit status',frame.evidence.tool&&frame.evidence.tool.exit_code],['Test result',frame.evidence.test&&frame.evidence.test.status],['Usage',frame.evidence.usage.length?'Recorded':'None']];if(worker.role==='builder'&&frame.evidence.blocker){data.push(['Reason',frame.evidence.blocker.reason],['Platform error',frame.evidence.blocker.platform_error],['Codex started',frame.evidence.blocker.codex_started?'Yes':'No']);}frame.evidence.files.forEach(file=>data.push([file.kind+' '+file.path,(file.before_sha256||'new')+' → '+(file.after_sha256||'deleted')]));return data}function inspectBuilding(frame,id){const e=frame.evidence,data=[['State',label(frame.buildings[id])]];if(id==='code_factory')data.push(['Active worker',frame.workers.find(worker=>worker.location==='code_factory')?.role||'None'],['Current task',frame.tasks[0]?.title||'None'],['Runner identity',frame.hud.runner_identity],['Tool status',e.tool&&e.tool.status],['Tool exit status',e.tool&&e.tool.exit_code],['Latest evidence',e.files.length?e.files[e.files.length-1].path:(e.blocker&&e.blocker.reason)||'None']);if(id==='testing_facility')data.push(['Test command',e.test&&e.test.command_id],['Exit code',e.test&&e.test.exit_code],['Duration ms',e.test&&e.test.duration_ms],['Stdout',e.test&&e.test.stdout],['Stderr',e.test&&e.test.stderr],['Result',e.test&&e.test.status]);if(id==='deployment_tower'){const preview=e.artifact&&e.artifact.id?{label:'Open preview',url:'/agentland/sessions/'+frame.session.id+'/artifacts/'+e.artifact.id+'/preview/'}:'None';data.push(['Artifact path',e.artifact&&e.artifact.path],['SHA-256',e.artifact&&e.artifact.sha256],['Producing worker',e.artifact&&e.artifact.worker],['Source task',e.artifact&&e.artifact.task_id],['Preview',preview])}return data}function select(kind,id){selection={kind,id};render()}function render(){const frame=frames[index];slider.value=index+1;position.textContent='Event '+(index+1)+' / '+frames.length;document.getElementById('mission').textContent=frame.session.mission;document.getElementById('metrics').textContent='STATUS '+frame.hud.session_status.toUpperCase()+' · TASKS '+frame.hud.tasks_complete+' · ACTIVE '+frame.hud.active_workers+' · BLOCKED '+frame.hud.blocked_workers+' · EVENTS '+frame.hud.event_count+' · RUNNER '+frame.hud.runner_identity+' · COST '+frame.hud.estimated_cost+' · SEQ '+frame.hud.last_sequence;Object.entries(frame.buildings).forEach(([id,status])=>{const el=document.getElementById(id);el.className='building '+id+' '+status;el.querySelector('small').textContent=label(status);el.onclick=()=>select('building',id)});const workers=document.getElementById('workers'),workerList=document.getElementById('worker-list'),feed=document.getElementById('feed');workers.replaceChildren();workerList.replaceChildren();feed.replaceChildren();frame.workers.forEach(worker=>{const token=document.createElement('button');token.className='worker '+worker.status;token.dataset.location=worker.location;token.textContent=worker.role.toUpperCase();token.onclick=()=>select('worker',worker.role);workers.appendChild(token);const option=document.createElement('button');option.textContent=worker.role+' · '+worker.status;option.setAttribute('aria-selected',selection.kind==='worker'&&selection.id===worker.role);option.onclick=()=>select('worker',worker.role);workerList.appendChild(option)});frame.activity.forEach(event=>{const li=document.createElement('li'),meta=document.createElement('span');meta.className='seq';meta.textContent='#'+event.sequence+' '+event.type;li.append(meta,document.createElement('br'),document.createTextNode(event.summary));feed.appendChild(li)});const title=document.getElementById('inspection-title'),detail=document.getElementById('inspection');detail.replaceChildren();let rows;if(selection.kind==='worker'){const worker=frame.workers.find(item=>item.role===selection.id)||frame.workers[0];title.textContent=(worker?worker.role:'Worker').toUpperCase();rows=worker?inspectWorker(frame,worker):[['Status','Not yet arrived']]}else{title.textContent=label(selection.id);rows=inspectBuilding(frame,selection.id)}rows.forEach(([key,value])=>detail.append(...row(key,value)))}function step(delta){index=Math.max(0,Math.min(frames.length-1,index+delta));render()}document.getElementById('previous').onclick=()=>step(-1);document.getElementById('next').onclick=()=>step(1);slider.oninput=event=>{index=Number(event.target.value)-1;render()};document.getElementById('play').onclick=()=>{if(timer){clearInterval(timer);timer=null;document.getElementById('play').textContent='Play';return}document.getElementById('play').textContent='Pause';timer=setInterval(()=>{if(index>=frames.length-1){clearInterval(timer);timer=null;document.getElementById('play').textContent='Play';return}step(1)},700)};render();
</script><script>
const liveBadge=document.createElement('output');liveBadge.id='new-confirmed';document.querySelector('.controls').appendChild(liveBadge);setInterval(async()=>{try{const known=frames.length?frames[frames.length-1].hud.last_sequence:0;const events=await fetch('/agentland/sessions/'+frames[0].session.id+'/events/?after_sequence='+known).then(r=>r.json());if(!events.events.length)return;const wasLatest=index===frames.length-1;const state=await fetch('/agentland/sessions/'+frames[0].session.id+'/city-state/').then(r=>r.json());frames.push(...state.frames.slice(frames.length));if(wasLatest){index=frames.length-1;liveBadge.textContent='';render()}else liveBadge.textContent=(frames.length-index-1)+' new confirmed events'}catch(error){/* The last confirmed frame remains visible. */}},750);const auto=new URLSearchParams(location.search).get('autostart');if(auto){fetch('/agentland/sessions/'+frames[0].session.id+'/dispatch-builder/',{method:'POST',headers:{'Content-Type':'application/json','Idempotency-Key':crypto.randomUUID()},body:JSON.stringify({runner_identity:auto})})}
</script><script>
const viewerKey='agentland-viewer-id';const viewerId=localStorage.getItem(viewerKey)||crypto.randomUUID();localStorage.setItem(viewerKey,viewerId);const contribution=document.createElement('button');contribution.type='button';contribution.textContent='Add verification request';document.querySelector('.controls').appendChild(contribution);contribution.onclick=async()=>{const request=prompt('Verify that strokes remain isolated between Room A and Room B.','Verify that strokes remain isolated between Room A and Room B.');if(!request)return;await fetch('/agentland/sessions/'+frames[0].session.id+'/verification-request/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({request})})};setInterval(()=>fetch('/agentland/sessions/'+frames[0].session.id+'/presence/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({viewer_id:viewerId,label:'Observer'})}),10000);fetch('/agentland/sessions/'+frames[0].session.id+'/presence/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({viewer_id:viewerId,label:'Observer'})});
</script><script>
contribution.onclick=async()=>{const request=prompt('Verify that strokes remain isolated between Room A and Room B.','Verify that strokes remain isolated between Room A and Room B.');if(!request)return;contribution.disabled=true;try{await fetch('/agentland/sessions/'+frames[0].session.id+'/verification-request/',{method:'POST',headers:{'Content-Type':'application/json','Idempotency-Key':crypto.randomUUID()},body:JSON.stringify({request})})}finally{contribution.disabled=false}};
</script></body></html>""")
